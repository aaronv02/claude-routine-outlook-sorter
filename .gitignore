node_modules/
.DS_Store
*.log

# Written by `npm run setup`. Holds a credential to the mailbox.
.env

# Holds the refresh token, the plan, the verdicts, and the digest. Never commit this.
routine/.local/
