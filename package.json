{
  "name": "claude-routine-outlook-sorter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Two scheduled Claude routines for an Outlook mailbox: category sorting that promotes itself into native Outlook rules, and an end-of-week summary of what is still waiting on a reply.",
  "license": "MIT",
  "scripts": {
    "setup": "tsx routine/setup.ts",
    "login": "tsx routine/sweep.ts login",
    "plan": "tsx routine/sweep.ts plan",
    "apply": "tsx routine/sweep.ts apply",
    "weekly": "tsx routine/weekly.ts",
    "test": "tsx test/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
